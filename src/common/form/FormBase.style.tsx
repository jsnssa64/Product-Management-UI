import styled from "@emotion/styled";
import { Grid } from '@mui/material';

const GridContainer = styled(Grid)`
    padding: 20px;
`

const FormContainer = styled(Grid)`
    padding: 20px;
`

export {GridContainer, FormContainer}